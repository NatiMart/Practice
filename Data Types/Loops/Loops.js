var nameArr = ['Apple', 'Banana', 'Orange', 'Peach', 'Strawberry', 'Plum'];
var fixednames = []
for(var index = 0; index < nameArr.length; index++){
    var fullName = nameArr[index];
    var vowels = nameArr['a', 'e', 'i', 'o', 'u']
    var nothing = nameArr['']
    var twoNames = fullName.replace(vowels, nothing)
    fixednames.push(twoNames)
    }
    console.log(fixednames);

    for (let index = 0; index < array.length; index++) {
        const element = array[index];
        
    }